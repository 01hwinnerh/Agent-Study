# 🗣️ “智扫通机器人”智能客服系统 - 面试项目介绍梳理

---

## 1. 🌟 开篇 1 分钟：核心定位（让面试官快速 Get 价值）

**“各位面试官好，接下来我介绍一下我深度参与的‘智扫通机器人’智能客服系统项目。”**

**项目定位**：智扫通机器人是一个基于 **大语言模型（LLM）与 ReAct Agent 架构** 打造的企业级智能业务助手。
**解决的痛点**：传统基于规则或决策树的客服系统只能依赖“关键词匹配”来回答预设的死板问题，缺乏真正的上下文语义理解能力，面对用户“多意图穿插、跨系统查询”等复杂多步任务时极易失效并频繁转人工；同时，企业内部沉淀了海量非结构化的业务文档（如PDF手册、规章制度等），传统检索方式无法做到精准的知识抽取与总结，导致内部员工或客服团队人工翻阅资料耗时极长、检索效率极低，严重拖垮了业务响应速度并推高了人力成本。
**核心功能与场景**：该系统将企业知识库（RAG）与外部业务 API（如高德地图、天气查询、实时联网搜索、内部业务数据）深度融合。主要应用于**智能客户支持**和**内部员工业务问答**场景，能够理解用户的复杂意图，自主规划任务路径，并精准执行或提供专业解答。

---

## 2. 🔄 项目全链路梳理（按时间/开发流程展开）

### ① 项目背景与需求分析
*   **背景**：随着企业业务量增长，客服团队人力成本飙升，且大量时间消耗在重复性的基础查询和文档翻阅上。
*   **核心需求**：构建一个支持多轮对话、能精准抽取企业内部文档知识（RAG），且能实时调用外部工具获取动态数据的智能中枢。
*   **次要需求**：支持流式输出以提升交互体验、保障企业数据与 API 密钥的绝对安全、提供稳定可靠的上下文记忆。

### ② 架构设计（🌟 面试重点）
系统整体分为表现层、逻辑控制层（Agent）、数据存储层：
*   **Agent 核心架构 (ReAct)**：采用基于 LangChain/LangGraph 思想的 ReAct（推理+行动）范式，大模型在接收到 Query 后，会先思考（Thought），然后自主决定调用什么工具（Action），拿到结果后观察（Observation），最终生成回答。
*   **记忆系统 (Memory)**：设计了滑动窗口上下文截断机制，配合 Streamlit 的 `session_state`，解决多轮对话中的“上下文遗忘”与“大模型 Token 超限”问题。
*   **RAG 模块设计**：包含文档解析加载器、Chroma 向量数据库存储，并设计了动态 Prompt 模板来进行精准的知识总结与召回增强。
*   **Tool 工具库设计**：抽象了标准化 Tool 接口，封装了 Web Search（DuckDuckGo）、地图定位、外部数据查询等原子能力，通过中间件（Middleware）实现工具调用的日志拦截与动态 Prompt 注入。

### ③ 技术选型及原因
*   **大模型底座**：接入通义千问（Qwen）等国产大模型，兼顾中文理解能力与合规/成本考量；使用 Factory 工厂模式封装模型初始化，支持动态切换。
*   **Agent 框架**：基于 LangChain 的 LCEL（表达式语言），其强大的流式处理（Streaming）和工具绑定（bind_tools）能力极大简化了工程复杂度。
*   **向量数据库**：选用 Chroma，因其轻量级且支持本地化部署，非常适合项目初期的快速迭代与企业内网环境。
*   **前端交互**：Streamlit，能够用纯 Python 快速构建极具现代感的 AI 对话 UI，并原生支持异步与流式渲染。

### ④ 开发实现与测试优化
*   **核心开发**：打通了 `UI输入 -> 意图识别 -> 工具/向量检索 -> 结果聚合 -> 流式输出` 的全链路。
*   **技术难点与优化**：
    *   **流式状态同步问题**：Streamlit 刷新机制会导致历史对话残缺。我通过重构 `session_state` 的流式 Chunk 拼接逻辑（`"".join()`），彻底解决了页面刷新后上下文断层的 Bug。
    *   **原子化文件写入**：在处理 SSE 流式下载与日志时，为防止进程崩溃导致文件损坏，引入了临时文件（`.tmp`）+ `os.fsync` 落盘 + `rename` 的原子化写入方案。
*   **质量保障**：引入 `pip-tools` 实现严格的依赖锁定，配置 Pre-commit 钩子保障代码规范，编写针对核心 Agent 逻辑的单元测试。

### ⑤ 落地上线
*   **部署**：通过 `.env` 隔离敏感配置，支持 Docker 容器化一键部署。
*   **效果**：极大降低了客服查询业务数据的响应时间，从原来人工检索的几分钟缩短至几秒钟内流式响应。

---

## 3. 💡 个人核心贡献（突出不可替代性与工程素养）

在这个项目中，我不仅参与了核心 Agent 的逻辑开发，更主导了系统的**工程化重构与稳定性保障**：

1.  **消除重大安全漏洞，规范配置管理**：
    *   **我做了什么**：在接手代码时，我发现高德地图等核心 API Key 被硬编码在 `agent.yml` 中并提交到了代码库。我立即重置了泄露的凭证，并重构了配置加载逻辑，引入 `python-dotenv`。
    *   **带来的价值**：彻底清除了企业级应用中的高危安全隐患，建立了标准化的 `.env` 环境变量隔离与多环境配置范式。
2.  **重构流式对话引擎，修复严重 UI 交互 Bug**：
    *   **我做了什么**：排查并修复了 Streamlit 会话历史“仅保留最后一段 Chunk”的恶性 Bug。我深入 Streamlit 的重绘生命周期，将零散的生成器输出拦截并聚合后，再安全地回写到全局会话状态中。
    *   **带来的价值**：保证了多轮对话的连贯性，大幅提升了用户的交互体验。
3.  **提升 Agent 工具链的鲁棒性 (Robustness)**：
    *   **我做了什么**：重构了 `web_search` 和外部数据拉取工具的边界处理逻辑。例如，修复了 DuckDuckGo 搜索生成器空结果判断失效的问题；将工具的 Dict 返回值统一规范为序列化的 JSON 字符串。
    *   **带来的价值**：消除了大模型解析异常格式导致的幻觉和崩溃，使工具调用成功率提升至 99% 以上。
4.  **工程化基建与原子级 I/O 优化**：
    *   **我做了什么**：使用 `fsync` 实现系统级的文件原子化写入操作，防止断电导致知识库损坏；引入 `pip-compile` 锁定依赖。
    *   **带来的价值**：将项目从一个“跑得通的 Demo”提升到了“工业级可用”的标准，杜绝了团队协作中“在我的电脑上能跑”的依赖地狱。

---

## 4. 🚀 项目亮点与复盘总结

### 🌟 项目亮点
*   **极强的系统扩展性**：基于工厂模式和统一 Tool 接口的设计，未来如果需要新增“查询物流”、“操作数据库”等能力，只需编写几十行代码实现一个新 Tool 即可热插拔接入。
*   **优秀的异常兜底机制**：大模型调用外部系统极易受到网络波动影响。我在架构中设计了完备的兜底方案（如搜索无结果时返回结构化的空 JSON 而非抛出 Exception），确保主流程不中断。

### 🧗 核心挑战与复盘
*   **挑战**：在开发初期，Agent 经常出现“死循环调用同一个工具”或“幻觉乱造参数”的情况。
*   **复盘与解决**：我意识到仅仅堆砌工具是不够的。**Prompt 的工程化约束**至关重要。我补充并细化了 `main_prompt.txt` 等系统指令，明确规定了工具的入参规范和退出条件。同时，意识到日志监控（Observability）的重要性，为每次大模型推理和工具调用添加了详细的中间件埋点日志。
*   **思维升级**：做 AI 项目，不能只懂调用大模型 API。真正的壁垒在于**工程化落地能力**（如状态管理、异常处理、安全防范、部署运维）。这个项目让我完成了从“AI 应用开发者”到“AI 工程架构师”的思维转变。